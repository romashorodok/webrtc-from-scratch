[project]
name = "webrtc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "maturin>=1.8.3",
    "webrtc_rs",
    "rav1e",
    "opus",
    "psutil>=7.0.0",
    "pytest>=8.3.5",
]
authors = [
    { name = "romashorodok", email = "108732029+romashorodok@users.noreply.github.com" }
]

[project.scripts]
wrtc-pymeta-compiler = "webrtc.compiler.module_compiler:main"

[tool.uv.workspace]
members = [
    "packages/*",
]

[tool.uv.sources]
webrtc_rs = { workspace = true }
rav1e = { workspace = true }
opus = { workspace = true }

[tool.uv]
config-settings = { build-args = '--profile=dev' }
package = true

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["webrtc*", "packages*", "pymeta*"]

[tool.setuptools.package-data]
"webrtc.compiler" = [
    "native_compiler/CMakeLists.txt",
    "native_compiler/main.c",
    "native_compiler/generator.c",
    "native_compiler/generator.h",
]

[tool.setuptools.exclude-package-data]
"webrtc.compiler" = ["native_compiler/kernel_e_extension.c.in"]

[tool.maturin]
module-name = "builder"
bindings = "pyo3"
features = ["pyo3/extension-module"]
